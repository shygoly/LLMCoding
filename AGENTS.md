# knowledge 目录工作规范

本目录用于沉淀 AI 软件工程相关的元方法、架构模式与决策记录。

## 目标

把原始讨论加工成可复用、可验证、可迭代的知识资产，而不是堆积聊天结论。

## 分类规则

### 1. inbox
适用于以下内容：
- 原始对话
- 还未验证的灵感
- 问题清单
- 尚不能稳定落地的方案

### 2. methods
适用于以下内容：
- 一组可重复执行的步骤
- 有明确输入、产物、边界
- 可被 AI 或人稳定复用

### 3. patterns
适用于以下内容：
- 某类架构或设计套路
- 多次出现的解决方案
- 强调结构、分层、组件关系

### 4. decisions
适用于以下内容：
- 已采纳或已否决的关键选择
- 需要记录原因、收益、代价
- 会影响后续实现方向

## 提炼流程

默认按以下流程处理新材料：

```text
raw note -> classify -> refine -> link -> review
```

### Step 1. classify
先判断材料属于 `inbox` / `method` / `pattern` / `decision`。

### Step 2. refine
按对应模板整理，不保留无信息量闲聊。

### Step 3. link
为新文档补充相关文档链接，至少链接一个已有文档。

### Step 4. review
若新结论与旧文档冲突，优先记录冲突，不直接覆盖。

## 编写要求

1. 每篇文档必须说明“适用场景”或“背景”
2. 每篇文档必须说明“风险、代价或边界”
3. 方法文档必须写步骤，不能只写观点
4. 决策文档必须写备选方案与选择原因
5. 优先小步增量修改，不大规模重写现有文档
6. 命名保持短、稳定、可检索

## 命名约定

- 方法：`verb-noun.md`
- 模式：`domain-pattern.md`
- 决策：`NNN-short-name.md`
- Inbox：`YYYY-MM-DD-topic.md`
- 回顾：`YYYY-MM-DD-review.md`

## 推荐工作方式

### 提炼新对话
1. 先存入 `inbox/`
2. 再提炼到 `methods/` 或 `patterns/`
3. 如果形成明确选择，再新增 `decisions/`

### 更新已有方法
1. 不直接覆盖原结论
2. 先补充“新增验证”或“边界修正”
3. 如变化较大，新增 review 文档说明原因

### 处理冲突
1. 标记冲突点
2. 链接相关文档
3. 在 review 中说明当前推荐版本

## OpenClaw 使用建议

处理本目录时，优先做四件事：
- 提炼：把长材料改写为结构化文档
- 归类：决定应该进入哪个目录
- 对比：检查是否与旧结论冲突
- 迭代：根据实践结果修正方法

不建议：
- 直接把聊天记录原样塞进 `methods/` 或 `patterns/`
- 在没有边界说明的情况下写“万能结论”
- 一次创建过多空白文档
